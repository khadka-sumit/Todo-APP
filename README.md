# Todo App 📝

A lightweight Flutter task management app with task completion and deletion.

## Features
- Add new tasks
- Mark tasks as completed (strikethrough)
- Delete tasks
- Clean Material 3 design


## Local Persistence
Tasks are serialized to JSON and persisted locally using `shared_preferences` so user data remains intact after restarting the app.

